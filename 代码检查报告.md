# 课程代码检查报告

## 检查范围
- 正式课程目录：CSharp课程、CPP课程
- 示例代码文件：24 个
- 逐行讲解文件：24 个

## 自动静态检查
- 未完成标记：TODO / FIXME / 下面还可以 / 演示退出逻辑
- 乱码替换字符
- 大括号数量平衡
- 每个示例是否配套 CodeWalkthrough.md

## 检查结果
正式课程目录内未发现上述静态问题。

## 编译环境说明
本机可以找到 dotnet 运行时，但没有 .NET SDK；没有找到 g++、clang++、cl 或 cmake。因此本次不能在本机完成真实编译运行验证。

## 已修正内容
- C# 第 7、8、11 课已补成可直接运行的完整示例。
- C# 最终项目已加入成绩输入 TryParse 循环校验，避免输入非法内容时崩溃。
- C++ 最终项目已补全完整增删查排、保存和读取功能。
- 每个正式示例目录均已生成 CodeWalkthrough.md 逐行讲解。

## 建议清理项
课程包根目录存在隐藏/工具目录：.temp_dotnet_run、.sixth、.vscode。其中 .temp_dotnet_run 是验证时产生的临时目录，不属于正式课程内容；如需打包发布，建议删除。

## 环境安装脚本

已生成一键脚本：

- 安装环境并测试课程.ps1
- 安装环境并运行测试说明.md

当前 Codex 会话里，联网提升权限命令被自动审批服务拒绝，因此我无法直接替你下载安装。脚本已通过 PowerShell 语法检查。你可以在本机 PowerShell 中进入课程目录后运行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\安装环境并测试课程.ps1
```

脚本会把 .NET SDK 和 C++ 编译器安装到当前工作区 .tools 目录，并生成 真实编译测试报告.md。
