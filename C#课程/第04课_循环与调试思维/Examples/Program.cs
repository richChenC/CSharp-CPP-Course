// 引入基础命名空间。
using System;
// 空行把变量声明和循环逻辑分开。

// 定义累加结果变量，初始值为 0。
int sum = 0;
// 从 1 循环到 100，每轮 i 自增 1。
for (int i = 1; i <= 100; i++)
// 循环体开始。
{
// 把当前数字累加到总和里。
    sum += i;
// 循环体结束。
}
// 输出最终结果，验证循环逻辑是否正确。
Console.WriteLine($"1 到 100 的和是：{sum}");