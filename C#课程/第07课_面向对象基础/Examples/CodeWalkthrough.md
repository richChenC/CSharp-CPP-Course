# 逐行代码讲解

这个文件用于配合示例代码学习。代码文件里已经有中文注释，这里再按行号解释每一行，方便检查和复习。

| 行号 | 代码 | 说明 |
|---:|---|---|
| 1 | `// 引入基础命名空间，后面要使用 Console 输出内容。` | 注释：引入基础命名空间，后面要使用 Console 输出内容。 |
| 2 | `using System;` | 引入命名空间或声明使用范围，减少后续代码的书写成本。 |
| 4 | `// 创建一个学生对象，并通过构造方法传入姓名和年龄。` | 注释：创建一个学生对象，并通过构造方法传入姓名和年龄。 |
| 5 | `Student student = new Student("小明", 18);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 6 | `// 调用对象自己的方法，让学生完成自我介绍。` | 注释：调用对象自己的方法，让学生完成自我介绍。 |
| 7 | `student.Introduce();` | 这一行用于组织代码结构或完成当前小步骤。 |
| 9 | `// 定义一个学生类，表示现实中的学生对象。` | 注释：定义一个学生类，表示现实中的学生对象。 |
| 10 | `class Student` | 定义一种数据结构或对象类型，是面向对象/结构化设计的基础。 |
| 11 | `{` | 代码块开始，表示接下来的代码属于同一个作用域。 |
| 12 | `    // 公开姓名属性，允许外部读取和修改。` | 注释：公开姓名属性，允许外部读取和修改。 |
| 13 | `    public string Name { get; set; }` | 这一行用于组织代码结构或完成当前小步骤。 |
| 15 | `    // 公开年龄属性，便于对象保存自己的状态。` | 注释：公开年龄属性，便于对象保存自己的状态。 |
| 16 | `    public int Age { get; set; }` | 这一行用于组织代码结构或完成当前小步骤。 |
| 18 | `    // 定义构造方法，在创建对象时初始化姓名和年龄。` | 注释：定义构造方法，在创建对象时初始化姓名和年龄。 |
| 19 | `    public Student(string name, int age)` | 这一行用于组织代码结构或完成当前小步骤。 |
| 20 | `    {` | 代码块开始，表示接下来的代码属于同一个作用域。 |
| 21 | `        // 把传入的姓名保存到当前对象的 Name 属性里。` | 注释：把传入的姓名保存到当前对象的 Name 属性里。 |
| 22 | `        Name = name;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 23 | `        // 把传入的年龄保存到当前对象的 Age 属性里。` | 注释：把传入的年龄保存到当前对象的 Age 属性里。 |
| 24 | `        Age = age;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 25 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 27 | `    // 定义成员方法，用来输出当前学生的信息。` | 注释：定义成员方法，用来输出当前学生的信息。 |
| 28 | `    public void Introduce()` | 这一行用于组织代码结构或完成当前小步骤。 |
| 29 | `    {` | 代码块开始，表示接下来的代码属于同一个作用域。 |
| 30 | `        // 使用字符串插值输出对象当前保存的姓名和年龄。` | 注释：使用字符串插值输出对象当前保存的姓名和年龄。 |
| 31 | `        Console.WriteLine($"我是{Name}，今年{Age}岁");` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 32 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 33 | `}` | 代码块结束，表示当前作用域到这里结束。 |
