# 逐行代码讲解

这个文件用于配合示例代码学习。代码文件里已经有中文注释，这里再按行号解释每一行，方便检查和复习。

| 行号 | 代码 | 说明 |
|---:|---|---|
| 1 | `// 引入基础命名空间，后面要用到 Console 和基本类型。` | 注释：引入基础命名空间，后面要用到 Console 和基本类型。 |
| 2 | `using System;` | 引入命名空间或声明使用范围，减少后续代码的书写成本。 |
| 3 | `// 空行用于分隔逻辑块，提升可读性。` | 注释：空行用于分隔逻辑块，提升可读性。 |
| 5 | `// 先提示用户输入姓名，这里使用 Write 让光标停留在同一行。` | 注释：先提示用户输入姓名，这里使用 Write 让光标停留在同一行。 |
| 6 | `Console.Write("请输入姓名：");` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 7 | `// 读取用户输入，如果为空就使用“未知”作为兜底值。` | 注释：读取用户输入，如果为空就使用“未知”作为兜底值。 |
| 8 | `string name = Console.ReadLine() ?? "未知";` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 9 | `// 继续提示用户输入年龄。` | 注释：继续提示用户输入年龄。 |
| 10 | `Console.Write("请输入年龄：");` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 11 | `// 把字符串转成整数；如果没有输入，就默认当作 0。` | 注释：把字符串转成整数；如果没有输入，就默认当作 0。 |
| 12 | `int age = int.Parse(Console.ReadLine() ?? "0");` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 13 | `// 使用字符串插值把姓名和年龄拼接成一条完整信息。` | 注释：使用字符串插值把姓名和年龄拼接成一条完整信息。 |
| 14 | `Console.WriteLine($"你好，{name}，你今年 {age} 岁。");` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
