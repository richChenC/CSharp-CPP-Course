# 逐行代码讲解

这个文件用于配合示例代码学习。代码文件里已经有中文注释，这里再按行号解释每一行，方便检查和复习。

| 行号 | 代码 | 说明 |
|---:|---|---|
| 1 | `// 包含输入输出功能。` | 注释：包含输入输出功能。 |
| 2 | `#include &lt;iostream&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 3 | `// 简化标准库使用。` | 注释：简化标准库使用。 |
| 4 | `using namespace std;` | 引入命名空间或声明使用范围，减少后续代码的书写成本。 |
| 5 | `// 空行把函数定义和 main 分开。` | 注释：空行把函数定义和 main 分开。 |
| 7 | `// 定义一个引用参数函数，用于交换两个整数。` | 注释：定义一个引用参数函数，用于交换两个整数。 |
| 8 | `void swapValue(int& a, int& b) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 9 | `// 先保存第一个值，防止交换时丢失。` | 注释：先保存第一个值，防止交换时丢失。 |
| 10 | `    int temp = a;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 11 | `// 把第二个值赋给第一个参数。` | 注释：把第二个值赋给第一个参数。 |
| 12 | `    a = b;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 13 | `// 把临时变量里的原值赋给第二个参数。` | 注释：把临时变量里的原值赋给第二个参数。 |
| 14 | `    b = temp;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 15 | `// 交换函数结束。` | 注释：交换函数结束。 |
| 16 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 17 | `// 空行分隔函数和主程序。` | 注释：空行分隔函数和主程序。 |
| 19 | `// 程序入口。` | 注释：程序入口。 |
| 20 | `int main() {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 21 | `// 准备两个待交换的变量。` | 注释：准备两个待交换的变量。 |
| 22 | `    int x = 3, y = 5;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 23 | `// 调用交换函数。` | 注释：调用交换函数。 |
| 24 | `    swapValue(x, y);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 25 | `// 输出交换后的结果。` | 注释：输出交换后的结果。 |
| 26 | `    cout &lt;&lt; x &lt;&lt; " " &lt;&lt; y &lt;&lt; endl;` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 27 | `// 正常退出。` | 注释：正常退出。 |
| 28 | `    return 0;` | 返回结果或结束当前函数。 |
| 29 | `// 主函数结束。` | 注释：主函数结束。 |
| 30 | `}` | 代码块结束，表示当前作用域到这里结束。 |
