# 逐行代码讲解

这个文件用于配合示例代码学习。代码文件里已经有中文注释，这里再按行号解释每一行，方便检查和复习。

| 行号 | 代码 | 说明 |
|---:|---|---|
| 1 | `// 引入排序算法，后面按姓名排序会用到 sort。` | 注释：引入排序算法，后面按姓名排序会用到 sort。 |
| 2 | `#include &lt;algorithm&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 3 | `// 引入文件流，后面保存和读取通讯录数据会用到。` | 注释：引入文件流，后面保存和读取通讯录数据会用到。 |
| 4 | `#include &lt;fstream&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 5 | `// 引入输入输出流，后面使用 cout 和 getline。` | 注释：引入输入输出流，后面使用 cout 和 getline。 |
| 6 | `#include &lt;iostream&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 7 | `// 引入字符串类型，联系人姓名、电话、邮箱都用 string 保存。` | 注释：引入字符串类型，联系人姓名、电话、邮箱都用 string 保存。 |
| 8 | `#include &lt;string&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 9 | `// 引入动态数组 vector，用来保存多条联系人记录。` | 注释：引入动态数组 vector，用来保存多条联系人记录。 |
| 10 | `#include &lt;vector&gt;` | 引入 C++ 标准库头文件，让后续代码可以使用对应功能。 |
| 11 | `// 使用标准命名空间，减少 std:: 前缀，让入门代码更清爽。` | 注释：使用标准命名空间，减少 std:: 前缀，让入门代码更清爽。 |
| 12 | `using namespace std;` | 引入命名空间或声明使用范围，减少后续代码的书写成本。 |
| 14 | `// 定义联系人结构体，一条 Contact 就是一条通讯录数据。` | 注释：定义联系人结构体，一条 Contact 就是一条通讯录数据。 |
| 15 | `struct Contact {` | 定义一种数据结构或对象类型，是面向对象/结构化设计的基础。 |
| 16 | `    // 保存联系人姓名。` | 注释：保存联系人姓名。 |
| 17 | `    string name;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 18 | `    // 保存联系人电话。` | 注释：保存联系人电话。 |
| 19 | `    string phone;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 20 | `    // 保存联系人邮箱。` | 注释：保存联系人邮箱。 |
| 21 | `    string email;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 22 | `};` | 代码块结束，表示当前作用域到这里结束。 |
| 24 | `// 定义数据文件名，集中管理，后续想改文件名只改这一处。` | 注释：定义数据文件名，集中管理，后续想改文件名只改这一处。 |
| 25 | `const string DATA_FILE = "contacts.csv";` | 这一行用于组织代码结构或完成当前小步骤。 |
| 27 | `// 显示主菜单，负责把用户可以选择的功能打印出来。` | 注释：显示主菜单，负责把用户可以选择的功能打印出来。 |
| 28 | `void ShowMenu() {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 29 | `    // 打印系统标题。` | 注释：打印系统标题。 |
| 30 | `    cout &lt;&lt; "\n======== 通讯录管理系统 ========\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 31 | `    // 打印新增联系人菜单项。` | 注释：打印新增联系人菜单项。 |
| 32 | `    cout &lt;&lt; "1. 添加联系人\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 33 | `    // 打印显示全部联系人菜单项。` | 注释：打印显示全部联系人菜单项。 |
| 34 | `    cout &lt;&lt; "2. 显示全部联系人\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 35 | `    // 打印查询联系人菜单项。` | 注释：打印查询联系人菜单项。 |
| 36 | `    cout &lt;&lt; "3. 查询联系人\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 37 | `    // 打印按姓名排序菜单项。` | 注释：打印按姓名排序菜单项。 |
| 38 | `    cout &lt;&lt; "4. 按姓名排序\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 39 | `    // 打印删除联系人菜单项。` | 注释：打印删除联系人菜单项。 |
| 40 | `    cout &lt;&lt; "5. 删除联系人\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 41 | `    // 打印保存并退出菜单项。` | 注释：打印保存并退出菜单项。 |
| 42 | `    cout &lt;&lt; "6. 保存并退出\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 43 | `    // 提示用户输入选择。` | 注释：提示用户输入选择。 |
| 44 | `    cout &lt;&lt; "请选择：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 45 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 47 | `// 添加联系人，使用引用传参是为了直接修改原来的 vector。` | 注释：添加联系人，使用引用传参是为了直接修改原来的 vector。 |
| 48 | `void AddContact(vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 49 | `    // 创建一个临时联系人对象，用来接收本次输入。` | 注释：创建一个临时联系人对象，用来接收本次输入。 |
| 50 | `    Contact c;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 51 | `    // 提示用户输入姓名。` | 注释：提示用户输入姓名。 |
| 52 | `    cout &lt;&lt; "请输入姓名：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 53 | `    // 读取一整行姓名，支持中文和空格。` | 注释：读取一整行姓名，支持中文和空格。 |
| 54 | `    getline(cin, c.name);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 55 | `    // 提示用户输入电话。` | 注释：提示用户输入电话。 |
| 56 | `    cout &lt;&lt; "请输入电话：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 57 | `    // 读取电话字符串。` | 注释：读取电话字符串。 |
| 58 | `    getline(cin, c.phone);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 59 | `    // 提示用户输入邮箱。` | 注释：提示用户输入邮箱。 |
| 60 | `    cout &lt;&lt; "请输入邮箱：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 61 | `    // 读取邮箱字符串。` | 注释：读取邮箱字符串。 |
| 62 | `    getline(cin, c.email);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 63 | `    // 把联系人追加到通讯录列表末尾。` | 注释：把联系人追加到通讯录列表末尾。 |
| 64 | `    contacts.push_back(c);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 65 | `    // 给用户一个操作成功反馈。` | 注释：给用户一个操作成功反馈。 |
| 66 | `    cout &lt;&lt; "添加成功。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 67 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 69 | `// 显示一个联系人，单独封装可以避免重复写输出格式。` | 注释：显示一个联系人，单独封装可以避免重复写输出格式。 |
| 70 | `void PrintContact(const Contact& c, int index) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 71 | `    // 输出序号，方便用户知道这是第几条。` | 注释：输出序号，方便用户知道这是第几条。 |
| 72 | `    cout &lt;&lt; index &lt;&lt; ". ";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 73 | `    // 输出姓名。` | 注释：输出姓名。 |
| 74 | `    cout &lt;&lt; "姓名：" &lt;&lt; c.name &lt;&lt; " \| ";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 75 | `    // 输出电话。` | 注释：输出电话。 |
| 76 | `    cout &lt;&lt; "电话：" &lt;&lt; c.phone &lt;&lt; " \| ";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 77 | `    // 输出邮箱并换行。` | 注释：输出邮箱并换行。 |
| 78 | `    cout &lt;&lt; "邮箱：" &lt;&lt; c.email &lt;&lt; "\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 79 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 81 | `// 显示全部联系人，使用 const 引用表示只读，不修改列表。` | 注释：显示全部联系人，使用 const 引用表示只读，不修改列表。 |
| 82 | `void ShowContacts(const vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 83 | `    // 如果列表为空，就直接提示并返回。` | 注释：如果列表为空，就直接提示并返回。 |
| 84 | `    if (contacts.empty()) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 85 | `        // 打印空数据提示。` | 注释：打印空数据提示。 |
| 86 | `        cout &lt;&lt; "当前没有联系人。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 87 | `        // 返回调用处，避免继续执行下面的遍历。` | 注释：返回调用处，避免继续执行下面的遍历。 |
| 88 | `        return;` | 返回结果或结束当前函数。 |
| 89 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 90 | `    // 使用普通 for 循环，方便输出从 1 开始的序号。` | 注释：使用普通 for 循环，方便输出从 1 开始的序号。 |
| 91 | `    for (size_t i = 0; i &lt; contacts.size(); ++i) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 92 | `        // 打印当前联系人。` | 注释：打印当前联系人。 |
| 93 | `        PrintContact(contacts[i], static_cast&lt;int&gt;(i + 1));` | 这一行用于组织代码结构或完成当前小步骤。 |
| 94 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 95 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 97 | `// 查询联系人，支持按姓名、电话、邮箱关键字搜索。` | 注释：查询联系人，支持按姓名、电话、邮箱关键字搜索。 |
| 98 | `void SearchContacts(const vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 99 | `    // 提示用户输入搜索关键字。` | 注释：提示用户输入搜索关键字。 |
| 100 | `    cout &lt;&lt; "请输入查询关键字：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 101 | `    // 定义关键字变量。` | 注释：定义关键字变量。 |
| 102 | `    string keyword;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 103 | `    // 读取用户输入的关键字。` | 注释：读取用户输入的关键字。 |
| 104 | `    getline(cin, keyword);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 105 | `    // 记录是否找到至少一个结果。` | 注释：记录是否找到至少一个结果。 |
| 106 | `    bool found = false;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 107 | `    // 遍历所有联系人。` | 注释：遍历所有联系人。 |
| 108 | `    for (size_t i = 0; i &lt; contacts.size(); ++i) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 109 | `        // 取出当前联系人，减少重复写 contacts[i]。` | 注释：取出当前联系人，减少重复写 contacts[i]。 |
| 110 | `        const Contact& c = contacts[i];` | 这一行用于组织代码结构或完成当前小步骤。 |
| 111 | `        // 判断姓名、电话或邮箱里是否包含关键字。` | 注释：判断姓名、电话或邮箱里是否包含关键字。 |
| 112 | `        if (c.name.find(keyword) != string::npos \|\| c.phone.find(keyword) != string::npos \|\| c.email.find(keyword) != string::npos) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 113 | `            // 打印匹配到的联系人。` | 注释：打印匹配到的联系人。 |
| 114 | `            PrintContact(c, static_cast&lt;int&gt;(i + 1));` | 这一行用于组织代码结构或完成当前小步骤。 |
| 115 | `            // 标记已经找到结果。` | 注释：标记已经找到结果。 |
| 116 | `            found = true;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 117 | `        }` | 代码块结束，表示当前作用域到这里结束。 |
| 118 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 119 | `    // 如果没有找到任何结果。` | 注释：如果没有找到任何结果。 |
| 120 | `    if (!found) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 121 | `        // 给出无结果提示。` | 注释：给出无结果提示。 |
| 122 | `        cout &lt;&lt; "没有找到匹配联系人。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 123 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 124 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 126 | `// 按姓名排序联系人，使用引用传参是为了让排序结果保存在原列表里。` | 注释：按姓名排序联系人，使用引用传参是为了让排序结果保存在原列表里。 |
| 127 | `void SortContacts(vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 128 | `    // 调用标准库 sort，并用 lambda 指定排序规则。` | 注释：调用标准库 sort，并用 lambda 指定排序规则。 |
| 129 | `    sort(contacts.begin(), contacts.end(), [](const Contact& a, const Contact& b) {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 130 | `        // 按姓名字典序从小到大排序。` | 注释：按姓名字典序从小到大排序。 |
| 131 | `        return a.name &lt; b.name;` | 返回结果或结束当前函数。 |
| 132 | `    });` | 这一行用于组织代码结构或完成当前小步骤。 |
| 133 | `    // 提示排序完成。` | 注释：提示排序完成。 |
| 134 | `    cout &lt;&lt; "排序完成。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 135 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 137 | `// 删除联系人，按电话精确匹配删除，电话通常更适合作唯一标识。` | 注释：删除联系人，按电话精确匹配删除，电话通常更适合作唯一标识。 |
| 138 | `void DeleteContact(vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 139 | `    // 提示用户输入要删除的电话。` | 注释：提示用户输入要删除的电话。 |
| 140 | `    cout &lt;&lt; "请输入要删除的联系人电话：";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 141 | `    // 定义电话变量。` | 注释：定义电话变量。 |
| 142 | `    string phone;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 143 | `    // 读取用户输入的电话。` | 注释：读取用户输入的电话。 |
| 144 | `    getline(cin, phone);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 145 | `    // 使用 remove_if 把要删除的联系人移动到末尾。` | 注释：使用 remove_if 把要删除的联系人移动到末尾。 |
| 146 | `    auto newEnd = remove_if(contacts.begin(), contacts.end(), [&](const Contact& c) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 147 | `        // 如果电话相等，就认为这条记录应该删除。` | 注释：如果电话相等，就认为这条记录应该删除。 |
| 148 | `        return c.phone == phone;` | 返回结果或结束当前函数。 |
| 149 | `    });` | 这一行用于组织代码结构或完成当前小步骤。 |
| 150 | `    // 判断是否真的找到了可删除的数据。` | 注释：判断是否真的找到了可删除的数据。 |
| 151 | `    if (newEnd == contacts.end()) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 152 | `        // 没有找到时提示用户。` | 注释：没有找到时提示用户。 |
| 153 | `        cout &lt;&lt; "没有找到该电话对应的联系人。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 154 | `        // 直接返回，不做 erase。` | 注释：直接返回，不做 erase。 |
| 155 | `        return;` | 返回结果或结束当前函数。 |
| 156 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 157 | `    // 真正删除 remove_if 移到末尾的元素。` | 注释：真正删除 remove_if 移到末尾的元素。 |
| 158 | `    contacts.erase(newEnd, contacts.end());` | 这一行用于组织代码结构或完成当前小步骤。 |
| 159 | `    // 提示删除成功。` | 注释：提示删除成功。 |
| 160 | `    cout &lt;&lt; "删除成功。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 161 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 163 | `// 保存联系人到 CSV 文件，实现简单的数据持久化。` | 注释：保存联系人到 CSV 文件，实现简单的数据持久化。 |
| 164 | `void SaveContacts(const vector&lt;Contact&gt;& contacts) {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 165 | `    // 打开输出文件流，默认会覆盖旧文件。` | 注释：打开输出文件流，默认会覆盖旧文件。 |
| 166 | `    ofstream out(DATA_FILE);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 167 | `    // 遍历所有联系人。` | 注释：遍历所有联系人。 |
| 168 | `    for (const Contact& c : contacts) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 169 | `        // 按 name,phone,email 的格式写入一行。` | 注释：按 name,phone,email 的格式写入一行。 |
| 170 | `        out &lt;&lt; c.name &lt;&lt; "," &lt;&lt; c.phone &lt;&lt; "," &lt;&lt; c.email &lt;&lt; "\n";` | 这一行用于组织代码结构或完成当前小步骤。 |
| 171 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 172 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 174 | `// 从 CSV 文件加载联系人，程序启动时调用。` | 注释：从 CSV 文件加载联系人，程序启动时调用。 |
| 175 | `vector&lt;Contact&gt; LoadContacts() {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 176 | `    // 创建空列表，用来保存读到的数据。` | 注释：创建空列表，用来保存读到的数据。 |
| 177 | `    vector&lt;Contact&gt; contacts;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 178 | `    // 打开输入文件流。` | 注释：打开输入文件流。 |
| 179 | `    ifstream in(DATA_FILE);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 180 | `    // 如果文件不存在或打不开，就返回空列表。` | 注释：如果文件不存在或打不开，就返回空列表。 |
| 181 | `    if (!in.is_open()) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 182 | `        // 返回空通讯录。` | 注释：返回空通讯录。 |
| 183 | `        return contacts;` | 返回结果或结束当前函数。 |
| 184 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 185 | `    // 定义一行文本变量。` | 注释：定义一行文本变量。 |
| 186 | `    string line;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 187 | `    // 逐行读取文件内容。` | 注释：逐行读取文件内容。 |
| 188 | `    while (getline(in, line)) {` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 189 | `        // 找到第一个逗号位置。` | 注释：找到第一个逗号位置。 |
| 190 | `        size_t first = line.find(',');` | 这一行用于组织代码结构或完成当前小步骤。 |
| 191 | `        // 找到第二个逗号位置。` | 注释：找到第二个逗号位置。 |
| 192 | `        size_t second = line.find(',', first + 1);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 193 | `        // 如果两个逗号都存在，说明格式基本正确。` | 注释：如果两个逗号都存在，说明格式基本正确。 |
| 194 | `        if (first != string::npos && second != string::npos) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 195 | `            // 创建联系人对象。` | 注释：创建联系人对象。 |
| 196 | `            Contact c;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 197 | `            // 截取姓名字段。` | 注释：截取姓名字段。 |
| 198 | `            c.name = line.substr(0, first);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 199 | `            // 截取电话字段。` | 注释：截取电话字段。 |
| 200 | `            c.phone = line.substr(first + 1, second - first - 1);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 201 | `            // 截取邮箱字段。` | 注释：截取邮箱字段。 |
| 202 | `            c.email = line.substr(second + 1);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 203 | `            // 把联系人加入列表。` | 注释：把联系人加入列表。 |
| 204 | `            contacts.push_back(c);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 205 | `        }` | 代码块结束，表示当前作用域到这里结束。 |
| 206 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 207 | `    // 返回加载完成的联系人列表。` | 注释：返回加载完成的联系人列表。 |
| 208 | `    return contacts;` | 返回结果或结束当前函数。 |
| 209 | `}` | 代码块结束，表示当前作用域到这里结束。 |
| 211 | `// 程序入口函数。` | 注释：程序入口函数。 |
| 212 | `int main() {` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 213 | `    // 启动程序时先从文件加载已有联系人。` | 注释：启动程序时先从文件加载已有联系人。 |
| 214 | `    vector&lt;Contact&gt; contacts = LoadContacts();` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 215 | `    // 使用死循环持续显示菜单，直到用户选择退出。` | 注释：使用死循环持续显示菜单，直到用户选择退出。 |
| 216 | `    while (true) {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 217 | `        // 打印主菜单。` | 注释：打印主菜单。 |
| 218 | `        ShowMenu();` | 这一行用于组织代码结构或完成当前小步骤。 |
| 219 | `        // 定义用户选择变量。` | 注释：定义用户选择变量。 |
| 220 | `        string choice;` | 声明变量、函数或返回类型，是程序保存数据和组织逻辑的关键。 |
| 221 | `        // 读取用户选择。` | 注释：读取用户选择。 |
| 222 | `        getline(cin, choice);` | 从控制台读取用户输入，让程序可以接收外部数据。 |
| 223 | `        // 如果选择 1，就添加联系人。` | 注释：如果选择 1，就添加联系人。 |
| 224 | `        if (choice == "1") {` | 控制程序流程，根据条件或循环次数决定代码如何执行。 |
| 225 | `            // 调用添加函数。` | 注释：调用添加函数。 |
| 226 | `            AddContact(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 227 | `        // 如果选择 2，就显示全部联系人。` | 注释：如果选择 2，就显示全部联系人。 |
| 228 | `        } else if (choice == "2") {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 229 | `            // 调用显示函数。` | 注释：调用显示函数。 |
| 230 | `            ShowContacts(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 231 | `        // 如果选择 3，就查询联系人。` | 注释：如果选择 3，就查询联系人。 |
| 232 | `        } else if (choice == "3") {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 233 | `            // 调用查询函数。` | 注释：调用查询函数。 |
| 234 | `            SearchContacts(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 235 | `        // 如果选择 4，就排序联系人。` | 注释：如果选择 4，就排序联系人。 |
| 236 | `        } else if (choice == "4") {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 237 | `            // 调用排序函数。` | 注释：调用排序函数。 |
| 238 | `            SortContacts(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 239 | `        // 如果选择 5，就删除联系人。` | 注释：如果选择 5，就删除联系人。 |
| 240 | `        } else if (choice == "5") {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 241 | `            // 调用删除函数。` | 注释：调用删除函数。 |
| 242 | `            DeleteContact(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 243 | `        // 如果选择 6，就保存并退出。` | 注释：如果选择 6，就保存并退出。 |
| 244 | `        } else if (choice == "6") {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 245 | `            // 保存当前联系人列表。` | 注释：保存当前联系人列表。 |
| 246 | `            SaveContacts(contacts);` | 这一行用于组织代码结构或完成当前小步骤。 |
| 247 | `            // 打印退出提示。` | 注释：打印退出提示。 |
| 248 | `            cout &lt;&lt; "已保存，程序退出。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 249 | `            // 跳出循环。` | 注释：跳出循环。 |
| 250 | `            break;` | 这一行用于组织代码结构或完成当前小步骤。 |
| 251 | `        // 其他输入都认为无效。` | 注释：其他输入都认为无效。 |
| 252 | `        } else {` | 这一行用于组织代码结构或完成当前小步骤。 |
| 253 | `            // 提醒用户重新输入。` | 注释：提醒用户重新输入。 |
| 254 | `            cout &lt;&lt; "输入无效，请重新选择。\n";` | 向控制台输出提示或结果，是用户看到程序反馈的地方。 |
| 255 | `        }` | 代码块结束，表示当前作用域到这里结束。 |
| 256 | `    }` | 代码块结束，表示当前作用域到这里结束。 |
| 257 | `    // 返回 0 表示程序正常结束。` | 注释：返回 0 表示程序正常结束。 |
| 258 | `    return 0;` | 返回结果或结束当前函数。 |
| 259 | `}` | 代码块结束，表示当前作用域到这里结束。 |
